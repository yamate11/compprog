#include <bits/stdc++.h>
#include <cassert>
typedef long long int ll;
using namespace std;

int main() {
  bool x;
  string s; cin >> s;
  for (int i = 0; i < s.size(); i++) {
    x = x || (s.at(i) % 2 == 1);
  }

  int z = 0;
  for (int i = 0; i < 10; i++) {
    z = z + 1;
  }
  {
    z = 3;
    int z = 5;
    for (int i = 0; i < 10; i++) {
      z = z + 1;
    }

  }
  

}
