#include <bits/stdc++.h>
#include <cassert>
typedef long long int ll;
using namespace std;

// Returns the Z-Array for string s
// i.e. a vector v of length s.size() such that
//   v[0] = 0
//   v[i] = max j such that s[i .. i+j) = s[0 .. j)     for i > 0
vector<int> zArray(string s) {
  vector<int> z(s.size());
  int x = 0;
  int y = 0;
  for (int i = 1; i < s.size(); i++) {
    if 
  }
  
}
