#include <bits/stdc++.h>
#include <cassert>
typedef long long int ll;
using namespace std;

int main(int argc, char *argv[]) {
  ios_base::sync_with_stdio(false);
  cin.tie(nullptr);
  cout << setprecision(20);

#if defined(DEBUG)
  cout << "debug is ON" << endl;
#else
  cout << "debug is OFF" << endl;
#endif

#if defined(CMPNAIVE)
  cout << "cmpnaive is ON" << endl;
#else
  cout << "cmpnaive is OFF" << endl;
#endif

  return 0;

}
